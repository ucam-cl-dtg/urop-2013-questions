{namespace view}

/**
 * Single set view
 * @param name
 * @param questions
 * @param plan
 * @param expectedDuration
 * @param id
 * @param setId
 */
{template .set}
	{call shared.set.single data="all" /}
{/template}