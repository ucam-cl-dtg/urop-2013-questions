{namespace questions.view.set.tab.plan}

/**
 * Full tab
 * @param plan
 */
{template .full}
<h4>Plan</h4>
{if $plan.data}
	<p>{$plan.data}</p>
{else}
	<p><i>--- No plan found ---</i></p>
{/if}
{/template}
